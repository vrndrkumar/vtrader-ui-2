# Copy to .env and fill in
PORT=3600
CANDLE_BASE_URL=https://data.vtrader.in
DB_HOST=
DB_NAME=
DB_USER=
# Quote the password if it contains # or spaces
DB_PASSWORD=""
# Comma-separated list of allowed CORS origins ("*" for all)
CORS_ORIGINS=*
